//INITIAL STATE 
const INITIAL_STATE = {
     username: "",
     id: "",
     profile_pic: ""
}

export default function reducer(state = INITIAL_STATE, action) {
     switch(action.type) {
          default: return state;
     }
}